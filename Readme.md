# CHECKPOINTING

## Objective 

* To have zerodown time failover of GPU workload
* Unified System/Kernel Level GPU Checkpointing (irrespective of application tf,torch)
* Distributed Checkpointing with its own FS storage with parity bits and hashing for corruption check